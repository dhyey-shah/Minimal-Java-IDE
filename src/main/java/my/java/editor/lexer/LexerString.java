package my.java.editor.lexer;

public class LexerString {
/*	
	private Lexer lexer;
	
	public LexerString() {
		lexer = new Lexer();
	}
	
	public void createToken(String line,int position) {
		int len = line.length();
		int startIndex=0,endIndex=-1;
		
		for(int i=position;i>=0;i--) {
		
			if(line.charAt(i) == ';') {
				startIndex = i+1;
				i=-1;
			}
		}
		
		for(int i=position;i<len;i++) {
			if(line.charAt(i) == ';') {
				endIndex = i;
				i=-1;
			}
		}
		
		System.out.print(endIndex);
		
		if(endIndex != -1) {
			lexer.tokenizeWord(line.substring(startIndex,endIndex+1));
		}
		
	}*/
}
